config:
  target: "http://localhost:9926"
  phases:
    - duration: 10
      arrivalRate: 10
  defaults:
    headers:
      Accept-Encoding: identity
scenarios:
  - flow:
      - get:
          url: "/Campsites"